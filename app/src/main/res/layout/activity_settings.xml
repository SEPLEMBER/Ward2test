<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settings_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0A0A">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/tv_title_settings"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Settings"
            android:textSize="20sp"
            android:textColor="#FFFFFF"
            android:paddingBottom="16dp" />

        <Button
            android:id="@+id/btn_choose_folder"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Choose EPUB folder (SAF)"
            android:textAllCaps="false"
            android:background="#BFBFBF"
            android:textColor="#000000"/>

        <TextView
            android:id="@+id/tv_folder_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No folder selected"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:textColor="#FFFFFF" />

        <Button
            android:id="@+id/btn_clear_folder"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Clear Selection"
            android:textAllCaps="false"
            android:layout_marginTop="8dp"
            android:background="#BFBFBF"
            android:textColor="#000000"/>

        <TextView
            android:id="@+id/tv_hint"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="20dp"
            android:text="Choose the root folder of the unpacked EPUB (contains META-INF and OPF). We store a persistent SAF permission."
            android:textColor="#DDDDDD"
            android:textSize="12sp" />
    </LinearLayout>
</ScrollView>
